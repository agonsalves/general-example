import {
    flexStart,
    white
} from 'utils/themer'

export const archiveThumbnailWrapperStyle = {
    backgroundColor: white,
    alignSelf: flexStart
}

export const archiveThumbnailStyle = {
    width: '100%',
    transition: 'opacity 250ms ease',
    hover: {
        opacity: .7
    }
}